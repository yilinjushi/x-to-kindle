name: Send Web To Kindle

on:
  workflow_dispatch:
    inputs:
      url:
        description: Article URL to send
        required: true
        type: string
      title:
        description: Optional title from Shortcut
        required: false
        type: string
      source:
        description: Source label
        required: false
        type: string

permissions:
  contents: write

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

concurrency:
  group: send-web-to-kindle
  cancel-in-progress: false

jobs:
  send-web-article:
    runs-on: ubuntu-latest
    env:
      KINDLE_EMAIL: ${{ secrets.KINDLE_EMAIL }}
      GMAIL_USER: ${{ secrets.GMAIL_USER }}
      GMAIL_APP_PASSWORD: ${{ secrets.GMAIL_APP_PASSWORD }}
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install Python dependencies
        run: pip install -r requirements.txt

      - name: Install Playwright browser
        run: python -m playwright install --with-deps chromium

      - name: Send web article
        run: >
          python send_web_article.py
          --url "${{ inputs.url }}"
          --title "${{ inputs.title }}"
          --source "${{ inputs.source || 'iphone_shortcut' }}"

      - name: Commit sent-web history and archive updates
        shell: bash
        run: |
          git add state/sent_web_articles.json archive/
          if git diff --cached --quiet; then
            echo "No sent-web history or archive changes to commit."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git commit -m "Update sent web article history and archive"
          git pull --rebase
          git push
